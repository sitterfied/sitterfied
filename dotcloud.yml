www:
  type: python
  config:
    python_version: v2.7
  approot: sitterfied
  systempackages:
    - htop
    - tmux

data:
  type: postgis

cache:
  type: redis

worker:
  type: python-worker
  config:
    python_version: v2.7
  systempackages:
    - htop
    - tmux
  postinstall: './worker_postinstall'