	  <!-- bookings_block -->
	  <div class="mob_tab_trigger">Bookings</div>
	  <div class="tab_content bookings_block" id="tab-3">
		<ul class="subtab_select">
		  <li><a href="#tab-1-1"><span>{{pendingRequests.length}}</span>Pending Requests</a></li>
		  <li><a href="#tab-1-2"><span>{{upcomingJobs.length}}</span>Upcoming Jobs</a></li>
		  <li><a href="#tab-1-3"><span>{{completedJobs.length}}</span>Completed Jobs</a></li>
		  <li><a href="#tab-1-4"><span>{{missedRequests.length}}</span>Missed Requests</a></li>
		  <li><a href="#tab-1-6"><span>{{declinedRequests.length}}</span>Declined Jobs</a></li>
		  <li><a href="#tab-1-7"><span>{{canceledRequests.length}}</span>Canceled Jobs</a></li>
		</ul>

		<div class="mob_tab_trigger active"><span>{{pendingRequests.length}}</span>Pending Requests</div>
		<!-- pending_requests_block -->
		<div class="subtab_content pending_requests_block" id="tab-1-1">
		  <ul class="booking_list">
            {{# each booking in pendingRequests }}
              {{ partial "booking" }}
            {{/ each}}
		  </ul>
		</div>
		<!-- /pending_jobs_block -->

		<div class="mob_tab_trigger "><span>{{upcomingJobs.length}}</span>Upcoming Jobs</div>
		<!-- pending_requests_block -->
		<div class="subtab_content upcoming_jobs_block" id="tab-1-2">
		  <ul class="booking_list">
            {{# each booking in upcomingJobs }}
              {{ partial "booking" }}
            {{/ each}}
		  </ul>
		</div>
		<!-- /upcomingJobs_block -->

		<div class="mob_tab_trigger"><span>{{completedJobs.length}}</span>Completed Jobs</div>
		<!-- completed_jobs_block -->
		<div class="subtab_content completed_jobs_block" id="tab-1-3">
          <ul class="booking_list">
		  {{# each booking in completedJobs }}
              {{ partial "booking" }}
          {{/ each}}
          </ul>
		</div>
		<!-- /completed_jobs_block -->

		<div class="mob_tab_trigger"><span>{{missedRequests.length}}</span>Missed Requests</div>
		<!-- missed_requests_block -->
		<div class="subtab_content missed_requests_block" id="tab-1-4">
          <ul class="booking_list">
		  {{# each booking in missedRequests }}
              {{ partial "booking" }}
          {{/ each}}
          </ul>
		</div>
		<!-- /missed_requests_block -->


        <div class="mob_tab_trigger"><span>2</span>Expired Requests</div>
        <!-- expired_requests_block -->
        <div class="subtab_content expired_requests_block" id="tab-1-5">
        </div>
        <!-- /expired_requests_block -->


		<div class="mob_tab_trigger"><span>{{declinedRequests.length}}</span>Declined Jobs</div>
		<!-- declined_jobs_block -->
		<div class="subtab_content declined_jobs_block" id="tab-1-6">
          <ul class="booking_list">
		  {{# each booking in declinedRequests }}
              {{ partial "booking" }}
          {{/ each}}
          </ul>
		</div>
		<!-- /declined_jobs_block -->

		<div class="mob_tab_trigger"><span>{{canceledRequests.length}}</span>Canceled Jobs</div>
		<!-- canceled_jobs_block -->
		<div class="subtab_content canceled_jobs_block" id="tab-1-7">
          <ul class="booking_list">
          {{# each booking in canceledRequests }}
              {{ partial "booking" }}
          {{/ each}}
          </ul>
		</div>
		<!-- /canceled_jobs_block -->
	  </div>
	  <!-- /bookings_block -->
