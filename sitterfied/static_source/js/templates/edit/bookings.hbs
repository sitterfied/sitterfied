<!-- bookings_block -->
{{# view Sitterfied.MobTriggerView }} Bookings {{/ view}}
	  <div class="tab_content bookings_block" id="tab-3">
		<ul class="subtab_select">
		  <li><a {{action zoomToPending}}><span>{{pendingRequests.length}}</span>Pending Requests</a></li>
		  <li><a {{action zoomToUpcoming}}><span>{{upcomingJobs.length}}</span>Upcoming Jobs</a></li>
		  <li><a {{action zoomToCompleted}}"><span>{{completedJobs.length}}</span>Completed Jobs</a></li>
		  <li><a {{action zoomToMissed}}><span>{{missedRequests.length}}</span>Missed Requests</a></li>
		  <li><a {{action zoomToDeclined}}><span>{{declinedRequests.length}}</span>Declined Jobs</a></li>
		  <li><a {{action zoomToCanceled }}><span>{{canceledRequests.length}}</span>Canceled Jobs</a></li>
		</ul>
        {{# view Sitterfied.MobTriggerView active=true }}
          <span>{{pendingRequests.length}}</span>
          Pending Requests
        {{/ view}}
		<!-- pending_requests_block -->
		<div class="subtab_content pending_requests_block" id="tab-1-1">
		  <ul class="booking_list">
            {{# each booking in pendingRequests }}
              {{ render "booking" booking }}
            {{/ each}}
		  </ul>
		</div>
		<!-- /pending_jobs_block -->

        {{# view Sitterfied.MobTriggerView }}
          <span>{{upcomingJobs.length}}</span>Upcoming Jobs
        {{/ view}}
		<!-- pending_requests_block -->
		<div class="subtab_content upcoming_jobs_block" id="tab-1-2">
		  <ul class="booking_list">
            {{# each booking in upcomingJobs }}
              {{ render "booking" booking }}
            {{/ each}}
		  </ul>
		</div>
		<!-- /upcomingJobs_block -->
        {{# view Sitterfied.MobTriggerView }}
		  <span>{{completedJobs.length}}</span>Completed Jobs
        {{/ view}}
		<!-- completed_jobs_block -->
		<div class="subtab_content completed_jobs_block" id="tab-1-3">
          <ul class="booking_list">
		  {{# each booking in completedJobs }}
                {{ render "booking" booking }}
          {{/ each}}
          </ul>
		</div>
		<!-- /completed_jobs_block -->
        {{# view Sitterfied.MobTriggerView }}
		  <span>{{missedRequests.length}}</span>Missed Requests
        {{/ view}}
		<!-- missed_requests_block -->
		<div class="subtab_content missed_requests_block" id="tab-1-4">
          <ul class="booking_list">
		  {{# each booking in missedRequests }}
              {{ render "booking" booking }}
          {{/ each}}
          </ul>
		</div>
		<!-- /missed_requests_block -->

        {{# view Sitterfied.MobTriggerView }}
          <span>0</span>Expired Requests
        {{/ view}}
        <div class="subtab_content expired_requests_block" id="tab-1-5">
        </div>
        -->

        {{# view Sitterfied.MobTriggerView }}
		  <span>{{declinedRequests.length}}</span>Declined Jobs
        {{/ view}}
		<!-- declined_jobs_block -->
		<div class="subtab_content declined_jobs_block" id="tab-1-6">
          <ul class="booking_list">
		  {{# each booking in declinedRequests }}
              {{ render "booking" booking }}
          {{/ each}}
          </ul>
		</div>
		<!-- /declined_jobs_block -->
        {{# view Sitterfied.MobTriggerView }}
		  <span>{{canceledRequests.length}}</span>Canceled Jobs
        {{/ view}}
		<!-- canceled_jobs_block -->
		<div class="subtab_content canceled_jobs_block" id="tab-1-7">
          <ul class="booking_list">
          {{# each booking in canceledRequests }}
              {{ render "booking" booking }}
          {{/ each}}
          </ul>
		</div>
		<!-- /canceled_jobs_block -->
	  </div>
	  <!-- /bookings_block -->
